
version: "3"
services:

  api:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongodb
    environment:
      - MONGO_URI=mongodb://mongodb:27017/proyectos_db

  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
